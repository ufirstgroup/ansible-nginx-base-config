---

# all files will be put into that folder
# to be included / used in regular config files
nginx_base_config_folder: /etc/nginx/base-config

# replace /etc/nginx/nginx.conf
nginx_base_config_replace: false

# remove some config file provided by the package
# like fastcgi.conf / fastcgi_params
nginx_base_config_cleanup: false
